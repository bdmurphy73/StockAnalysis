# SMTP configuration template
# Copy this to a file named .env (or export these as environment variables)
# Replace placeholders with your SMTP provider values

# SMTP server host (example: smtp.gmail.com)
SMTP_HOST=
# SMTP server port (587 for STARTTLS, 465 for SSL)
SMTP_PORT=587
# SMTP username (often your email address)
SMTP_USER=
# SMTP password or app-specific password
SMTP_PASS=
# Optional: from address to use in emails (defaults to SMTP_USER if empty)
SMTP_FROM=
# Recipient to receive automated backtest emails (comma-separated allowed)
SMTP_TO=
# Use STARTTLS? true/false (default true)
SMTP_TLS=true
# Use SSL (implicit TLS) on connect? true/false (default false). If true, SMTP_PORT often 465
SMTP_SSL=false

# Security note: keep this file private (do NOT commit credentials to git).
